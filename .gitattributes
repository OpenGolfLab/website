# Normalize line endings — store and check out as LF everywhere.
# Keeps the repo consistent and matches the Linux GitHub Actions build.
* text=auto eol=lf

# Binary assets — never modify their bytes.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
