# Screenshots

Drop real app screenshots here, using these exact filenames (referenced by the
site). Recommended: 1600×1000 JPG (16:10), optimized, under ~300 KB each.

| File                       | Used on        | Should show                                                    |
| -------------------------- | -------------- | ------------------------------------------------------------- |
| `home.jpg`                 | Home hero      | The home dashboard / session summary                          |
| `dispersion.jpg`           | Home           | Dispersion plot with target ellipse                           |
| `gapping_benchmarks.jpg`   | Home           | Club gapping bars with PGA Tour / handicap benchmark overlays |
| `live.jpg`                 | Home           | Live real-time dispersion during a session                    |
| `speed_training.jpg`       | Home           | Speed training: cruising speed, fatigue, progression          |

The `<Screenshot>` component locks a 16:10 aspect ratio, so images won't cause
layout shift even before you add them. Until the real files exist, the browser
shows the descriptive alt text.
